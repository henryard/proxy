proxy-providers:
  sublink:
    url: "__SUBSCRIBE_URL__"
    type: http
    interval: 86400
    health-check:
      enable: true
      url: https://www.gstatic.com/generate_204
      interval: 300
    exclude-filter: ".*(到期|重置|流量|公益|剩余|邮箱).*"

proxies:
  - {name: 🎯 Direct, type: direct}
