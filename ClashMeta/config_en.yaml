# ... other settings ...
# 代理节点 (由 Worker 动态填充)
proxies:
__PROXIES_PLACEHOLDER__

# 代理组 (Proxy Groups)
proxy-groups:
  - name: "PROXY"
    type: select
    # 在这里使用 'use' 关键字来引用所有由 Worker 生成的节点
    use:
      - ALL
# ...
